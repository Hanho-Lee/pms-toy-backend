server:
  port : 8080

spring:
  application:
    name: pms-toy

  datasource:
    url: jdbc:postgresql://localhost:5432/pms_toy
    username: ihanho
    password:
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.example.pms_toy
  configuration:
    map-underscore-to-camel-case: true